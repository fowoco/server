package com.fowoco.server.audit.domain;

public enum AuditTargetType {
    TASK,
    APPROVAL_REQUEST,
    EXTERNAL_SUBMISSION,
    EVIDENCE,
    FILE,
    WORKER_DOCUMENT,
    DOCUMENT_REQUEST_DRAFT
}
